apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: hrm-namespace
  labels:
    app: client
spec:
  type: ClusterIP
  selector:
    app: client
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80