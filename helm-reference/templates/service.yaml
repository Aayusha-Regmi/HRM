apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-reference.fullname" . }}
  labels:
    {{- include "helm-reference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-reference.selectorLabels" . | nindent 4 }}
