apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-reference.fullname" . }}-test-connection"
  labels:
    {{- include "helm-reference.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-reference.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
